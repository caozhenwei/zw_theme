<header class="header">
  <div class="blog-title">
    <img src="<%- partial('post/urlconvert', {tempUrl: theme.avatar_url}) %>" style="width: 150px;height: 150px">
    <a href="<%- url_for() %>" class="logo"><%= config.title %></a>
  </div>
  <nav class="navbar">
    <ul class="menu">
      <% for (name in theme.menu) { %>
        <li class="menu-item">
          <a href="<%- url_for(theme.menu[name]) %>" class="menu-item-link"><%= name %></a>
        </li>
      <% } %>
    </ul>
  </nav>
</header>